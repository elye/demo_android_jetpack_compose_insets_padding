<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.jetpackcomposeinsets">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.JetpackComposeInsets">
        <activity
            android:name=".NavigationBarImePaddingActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".NavigationBarPaddingActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".StatusBarPaddingActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".SystemBarsPaddingActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".FitSystemWindowActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".OriginalWindowActivity"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name"
            android:theme="@style/Theme.JetpackComposeInsets.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>